<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view="clr-namespace:KokoroIO.XamarinForms.Views"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    x:Class="KokoroIO.XamarinForms.Views.MessagesPage"
    Title="{Binding Title}">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style
                x:Key="bannerButtonStyle"
                TargetType="Button">
                <Setter
                    Property="Margin"
                    Value="8,4" />
                <Setter
                    Property="VerticalOptions"
                    Value="Start" />
                <Setter
                    Property="HorizontalOptions"
                    Value="FillAndExpand" />
                <Setter
                    Property="FontSize"
                    Value="Small" />
                <Setter
                    Property="BorderWidth"
                    Value="0" />
                <Setter
                    Property="BorderRadius"
                    Value="5" />
                <Setter
                    Property="TextColor"
                    Value="#fff" />
            </Style>
            <Style
                x:Key="footerButtonStyle"
                TargetType="Button">
                <Setter
                    Property="FontFamily"
                    Value="Monospace" />
                <Setter
                    Property="FontSize"
                    Value="Small" />
                <Setter
                    Property="TextColor"
                    Value="#ccc" />
                <Setter
                    Property="BackgroundColor"
                    Value="Transparent" />
                <Setter
                    Property="Margin"
                    Value="0" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition
                    Height="Auto" />
                <RowDefinition
                    Height="Auto" />
            </Grid.RowDefinitions>
            <view:MessageWebView
                LoadOlderCommand="{Binding PrependCommand}"
                RefreshCommand="{Binding RefreshCommand}"
                NavigatingCommand="{Binding OpenUrlCommand}"
                Messages="{Binding Messages}"
                SelectedMessage="{Binding SelectedMessage}" />
            <StackLayout
                HorizontalOptions="FillAndExpand"
                VerticalOptions="Start">
                <Button
                    Style="{StaticResource bannerButtonStyle}"
                    BackgroundColor="#4169E1"
                    Text="{Binding Room.UnreadCount,StringFormat='{}{0} new messages'}"
                    Command="{Binding ShowUnreadCommand}">
                    <Button.Triggers>
                        <DataTrigger
                            TargetType="Button"
                            Binding="{Binding Room.UnreadCount}"
                            Value="0">
                            <Setter
                                Property="IsVisible"
                                Value="False" />
                        </DataTrigger>
                    </Button.Triggers>
                </Button>
                <Button
                    Style="{StaticResource bannerButtonStyle}"
                    Text="Archived channel"
                    BackgroundColor="DarkOrange"
                    IsVisible="{Binding IsArchiveBannerShown}"
                    Command="{Binding ClearArchiveBannerCommand}" />
                <Button
                    Style="{StaticResource bannerButtonStyle}"
                    Text="Disconnected from server"
                    BackgroundColor="Khaki"
                    IsVisible="{Binding Application.IsDisconnected}"
                    Command="{Binding Application.ConnectCommand}" />
                <ListView
                    VerticalOptions="Start"
                    HorizontalOptions="End"
                    SeparatorVisibility="None"
                    InputTransparent="True"
                    ItemsSource="{Binding Notifications}">
                    <ListView.Style>
                        <Style
                            TargetType="ListView">
                            <Style.Triggers>
                                <DataTrigger
                                    TargetType="ListView"
                                    Binding="{Binding Notifications.Count}"
                                    Value="0">
                                    <Setter
                                        Property="IsVisible"
                                        Value="False" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.Style>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Grid
                                    HorizontalOptions="End">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition
                                            Width="Auto" />
                                        <ColumnDefinition />
                                    </Grid.ColumnDefinitions>
                                    <Image
                                        VerticalOptions="Center"
                                        WidthRequest="16"
                                        HeightRequest="16"
                                        Source="{Binding Profile.Avatar}" />
                                    <Label
                                        Grid.Column="1"
                                        FontSize="Micro"
                                        TextColor="Red"
                                        VerticalOptions="Center"
                                        VerticalTextAlignment="Center"
                                        Text="{Binding Room.DisplayName, StringFormat='posted to {0}'}" />
                                </Grid>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
            <ActivityIndicator
                VerticalOptions="Center"
                HorizontalOptions="Center"
                IsRunning="True"
                IsVisible="{Binding IsBusy}" />

            <StackLayout
                Grid.Row="1"
                HeightRequest="32"
                Orientation="Horizontal">

                <Button
                    Style="{StaticResource footerButtonStyle}"
                    WidthRequest="32"
                    Text="+"
                    Command="{Binding UploadImageCommand}"
                    IsVisible="{Binding SupportsImageUpload}" />

                <Button
                    Style="{StaticResource footerButtonStyle}"
                    WidthRequest="32"
                    Text="Ph"
                    Command="{Binding TakePhotoCommand}"
                    IsVisible="{Binding SupportsTakePhoto}" />
            </StackLayout>

            <Grid
                Grid.Row="2"
                Margin="4">
                <Grid.Triggers>
                    <DataTrigger
                        TargetType="Grid"
                        Binding="{Binding Room.IsArchived}"
                        Value="True">
                        <Setter
                            Property="IsVisible"
                            Value="False" />
                    </DataTrigger>
                </Grid.Triggers>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition
                        Width="Auto" />
                </Grid.ColumnDefinitions>
                <view:ExpandableEditor
                    x:Name="editor"
                    FontSize="Small"
                    HorizontalOptions="FillAndExpand"
                    Text="{Binding NewMessage}"
                    Placeholder="{Binding Room.Placeholder}"
                    PostCommand="{Binding PostCommand}"
                    FilePasted="ExpandableEditor_FilePasted" />
                <Button
                    Grid.Column="1"
                    Style="{StaticResource footerButtonStyle}"
                    Text="Go"
                    VerticalOptions="End"
                    WidthRequest="45"
                    Command="{Binding PostCommand}" />
            </Grid>
        </Grid>
    </ContentPage.Content>
</ContentPage>